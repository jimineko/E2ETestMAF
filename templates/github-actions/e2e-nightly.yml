name: Nightly E2E

on:
  schedule:
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: python -m pip install --upgrade pip
      - run: python -m pip install "git+https://github.com/jimineko/E2ETestMAF.git"
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: maf-e2e regression --target-repo . --environment staging
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-artifacts
          path: |
            .maf-e2e/regression/
            playwright-report/
            test-results/
