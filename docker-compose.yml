services:
  qa:
    build: .
    env_file:
      - path: .env
        required: false
    volumes:
      - ./artifacts:/app/artifacts
      - ./checkpoints:/app/checkpoints
      - ./auth:/app/auth:ro
    shm_size: 2gb
